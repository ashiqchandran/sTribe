<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Authentication</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style/user/home.css">
    
</head>
<body>
     <nav>
        <div class="logo"><img src="/images/logo.png" class="logo-class">STYLE TRIBE</div> 
        <div class="nav-links">
            <a href="/home">Home</a>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <a href="/shop">Shop</a>
            <!-- <a href="/productLoader" onclick="filterProducts('all')">Product</a> -->
            
            <% if (user ) { %>
                <a href="/cart" >Cart</a>
                <a href="/wishlist" >Wishlist</a>
                <a href="/orders" >orders</a>
                <li class="dropdown">
                    
                    &nbsp &nbsp
                    <a href="/profile" class="dropbtn">Account</a>
                    <!-- <div class="dropdown-content">
                        <a href="/address" class="dropbtn">address</a>
                        <a href="/profile">profile</a>
                        <a href="#" onclick="confirmLogout(event)">logout</a>
                    </div>
                    -->
                    
                </li>
                
            <% } else { %>
                <a href="/signin">Login</a>
            <% } %>

        </div>
        
        <div class="nav-icons">
           
            <% if (user) { %>
            <span> <img src="<%= user.image ? user.image : '/uploads/defaultProfileImage.jpg' %>" alt="User Profile" class="profile-image"></span>
            <% } %>
            <% if (locals.user) { %>
                <a href="#" class="sign-in-link"><%= locals.user.fullname %></a>
                <% } %>
            <!-- <span>🛒</span> -->
        </div>
    </nav>
</body>
</html>
<script>
    	function confirmLogout(event) {
        event.preventDefault(); // Prevent default link behavior

        Swal.fire({
            title: "Are you sure?",
            text: "You will be logged out!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#3085d6",
            cancelButtonColor: "#d33",
            confirmButtonText: "Yes, Logout"
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = "/logout"; // Redirect only if confirmed
            }
        });
    }
</script>