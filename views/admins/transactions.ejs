<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style/admin/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.12/cropper.min.js"></script>
    <title>Admin Transactions</title>

    <style>
      :root {
          --primary-color: #1a73e8; /* Blue */
          --secondary-color: #f5f7fa; /* Light gray */
          --text-color: #202124; /* Dark gray */
          --border-color: #dcdcdc; /* Light border */
          --success-color: #34a853; /* Green */
          --error-color: #ea4335; /* Red */
          --shadow: 0 4px 12px rgba(0, 0, 0, 0.1);
          --sidebar-width: 250px;
      }

   
      /* Admin container and layout */
      .admin-container {
          display: flex;
          width: 100%;
          min-height: 100vh;
      }


      .logout-btn:hover {
          background-color: #d32f2f;
      }

      /* Hamburger Menu for Mobile */
      .hamburger {
          display: block;
          cursor: pointer;
          background: none;
          border: none;
          padding: 10px;
          position: fixed;
          top: 20px;
          left: 20px;
          z-index: 1100;
          color: var(--primary-color);
          font-size: 24px;
      }

      /* Transaction Styles */
      h1, h2 {
          color: var(--primary-color);
          margin-bottom: 20px;
          font-weight: 600;
      }

      .service-table-box {
          max-width: 1200px;
          margin: 0 auto;
      }

      /* Filter Form Styles */
      #transactionFilterForm {
          background-color: #fff;
          padding: 25px;
          border-radius: 10px;
          box-shadow: var(--shadow);
          margin: 0 auto 30px;
          display: flex;
          flex-wrap: wrap;
          justify-content: space-between;
      }

      .form-group {
          flex: 1;
          min-width: 200px;
          margin: 0 10px;
      }

      label {
          display: block;
          font-weight: 500;
          margin-bottom: 8px;
          color: var(--text-color);
      }

      select, input[type="date"] {
          width: 100%;
          padding: 10px;
          margin-bottom: 20px;
          border: 1px solid var(--border-color);
          border-radius: 6px;
          font-size: 1rem;
          background-color: #fff;
          transition: border-color 0.3s ease, box-shadow 0.3s ease;
      }

      select:focus, input[type="date"]:focus {
          border-color: var(--primary-color);
          box-shadow: 0 0 8px rgba(26, 115, 232, 0.2);
          outline: none;
      }

      button {
          background-color: var(--primary-color);
          color: #fff;
          padding: 10px 20px;
          border: none;
          border-radius: 6px;
          font-size: 1rem;
          font-weight: 500;
          cursor: pointer;
          transition: background-color 0.3s ease, transform 0.2s ease;
      }

      button:hover {
          background-color: #1557b0; /* Darker blue */
          transform: translateY(-2px);
      }

      /* Transaction Table Styles */
      #transactionTable {
          margin-top: 20px;
          overflow-x: auto; /* Responsive horizontal scroll */
      }

      table {
          width: 100%;
          border-collapse: collapse;
          background-color: #fff;
          border-radius: 10px;
          box-shadow: var(--shadow);
          overflow: hidden;
      }

      thead {
          background-color: var(--primary-color);
          color: #fff;
      }

      th, td {
          padding: 15px;
          text-align: left;
          border-bottom: 1px solid var(--border-color);
      }

      th {
          font-weight: 600;
          font-size: 1.1rem;
      }

      tbody tr {
          transition: background-color 0.2s ease;
      }

      tbody tr:hover {
          background-color: #f9f9f9; /* Light hover effect */
      }

      td {
          font-size: 0.95rem;
          color: var(--text-color);
      }

      /* Pagination Controls */
      #paginationControls {
          display: flex;
          justify-content: center;
          align-items: center;
          margin: 20px 0;
      }

      #paginationControls button {
          background-color: var(--primary-color);
          color: white;
          border: none;
          padding: 8px 16px;
          margin: 0 10px;
          border-radius: 4px;
          cursor: pointer;
          transition: background-color 0.3s;
      }

      #paginationControls button:hover {
          background-color: #1557b0;
      }

      #paginationControls button:disabled {
          background-color: #cccccc;
          cursor: not-allowed;
      }

      #pageInfo {
          font-weight: 500;
          color: var(--text-color);
      }

      /* Items per page selector */
      #itemsPerPage {
          padding: 5px;
          border: 1px solid var(--border-color);
          border-radius: 4px;
          background-color: white;
      }

      /* Overlay for sidebar when active */
      .sidebar-overlay {
          display: none;
          position: fixed;
          top: 0;
          left: 0;
          right: 0;
          bottom: 0;
          background-color: rgba(0, 0, 0, 0.5);
          z-index: 900;
      }

      .sidebar-overlay.active {
          display: block;
      }

      /* Responsive Design */
      @media (min-width: 1201px) {
          .sidebar {
              left: 0;
          }
          .main-content {
              margin-left: var(--sidebar-width);
              width: calc(100% - var(--sidebar-width));
          }
          .hamburger {
              display: none;
          }
          .sidebar-overlay {
              display: none !important;
          }
      }

      @media (max-width: 768px) {
          .service-table-box {
              padding: 0;
          }

          h1 {
              font-size: 1.5rem;
              margin-top: 40px;
          }

          h2 {
              font-size: 1.3rem;
          }

          #transactionFilterForm {
              padding: 15px;
              flex-direction: column;
          }

          .form-group {
              margin: 0 0 15px 0;
              width: 100%;
          }

          select, input[type="date"] {
              font-size: 0.9rem;
          }

          button {
              font-size: 0.9rem;
              padding: 10px;
          }

          th, td {
              padding: 12px;
              font-size: 0.9rem;
          }
      }

      @media (max-width: 480px) {
          th, td {
              padding: 10px;
              font-size: 0.85rem;
          }

          table {
              font-size: 0.8rem;
          }
      }/* General Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: serif;
}

body {
    background-color: var(--secondary-color);
    line-height: 1.6;
    overflow-x: hidden;
}

/* Admin container and layout */
.admin-container {
    display: flex;
    width: 100%;
    min-height: 100vh;
}

/* Sidebar Styles */
.sidebar {
    width: var(--sidebar-width);
    background-color: #333;
    box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
    transition: all 0.3s ease;
    height: 100vh;
    position: fixed;
    top: 0;
    left: -250px;
    overflow-y: auto;
    z-index: 1000;
    color: white;
}

.sidebar.active {
    left: 0;
}

.sidebar-header h3 {
    font-size: 2rem; /* Increased font size */
    margin: 0;
}

.sidebar-menu {
    list-style: none;
    padding: 0;
    color: white;
}

.sidebar-menu li a {
    display: block;
    color: white;
    transition: all 0.3s ease;
    text-decoration: none;
 margin-left: 0;
    line-height: 25px;
    font-size: 1rem; /* Increased font size */
}

.sidebar-menu li a:hover, .sidebar-menu li a.active {
    background-color: rgba(217, 223, 232, 0.1);
    color: white;
    border-left: 4px solid var(--primary-color);
}

.sidebar-menu li a i {
    margin-right: 10px;
    width: 20px;
    text-align: center;
}

/* Main Content */
.main-content {
    flex: 1;
    padding: 20px;
    margin-left: 0;
    transition: all 0.3s ease;
    width: 100%;
}

.admin-info-box {
    background-color: #fff;
    padding: 15px;
    margin-bottom: 20px;
    border-radius: 8px;
    box-shadow: var(--shadow);
}

.top-header {
    display: flex;
    justify-content: flex-end;
}

.admin-info {
    display: flex;
    align-items: center;
}

.logout-btn {
    background-color: var(--error-color);
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
}

.logout-btn:hover {
    background-color: #d32f2f;
}

/* Transaction Styles */
h1, h2 {
    color: var(--primary-color);
    margin-bottom: 20px;
    font-weight: 600;
    font-size: 2rem; /* Increased font size */
}

.service-table-box {
    max-width: 1200px;
    margin: 0 auto;
}

/* Filter Form Styles */
#transactionFilterForm {
    background-color: #fff;
    padding: 25px;
    border-radius: 10px;
    box-shadow: var(--shadow);
    margin: 0 auto 30px;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
}

.form-group {
    flex: 1;
    min-width: 200px;
    margin: 0 10px;
}

label {
    display: block;
    font-weight: 500;
    margin-bottom: 8px;
    color: var(--text-color);
}

select, input[type="date"] {
    width: 100%;
    padding: 10px;
    margin-bottom: 20px;
    border: 1px solid var(--border-color);
    border-radius: 6px;
    font-size: 1rem;
    background-color: #fff;
    transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

select:focus, input[type="date"]:focus {
    border-color: var(--primary-color);
    box-shadow: 0 0 8px rgba(26, 115, 232, 0.2);
    outline: none;
}

button {
    background-color: var(--primary-color);
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

button:hover {
    background-color: #1557b0; /* Darker blue */
    transform: translateY(-2px);
}

/* Transaction Table Styles */
#transactionTable {
    margin-top: 20px;
    overflow-x: auto; /* Responsive horizontal scroll */
}

table {
    width: 100%;
    border-collapse: collapse;
    background-color: #fff;
    border-radius: 10px;
    box-shadow: var(--shadow);
    overflow: hidden;
}

thead {
    background-color: var(--primary-color);
    color: #fff;
}

th, td {
    padding: 15px;
    text-align: left;
    border-bottom: 1px solid var(--border-color);
    font-size: 1.1rem; /* Increased font size */
}

th {
    font-weight: 600;
    font-size: 1.2rem; /* Increased font size */
}

tbody tr {
    transition: background-color 0.2s ease;
}

tbody tr:hover {
    background-color: #f9f9f9; /* Light hover effect */
}

td {
    font-size: 1rem; /* Increased font size */
    color: var(--text-color);
}

/* Pagination Controls */
#paginationControls {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 20px 0;
}

#paginationControls button {
    background-color: var(--primary-color);
    color: white;
    border: none;
    padding: 8px 16px;
    margin: 0 10px;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s;
}

#paginationControls button:hover {
    background-color: #1557b0;
}

#paginationControls button:disabled {
    background-color: #cccccc;
    cursor: not-allowed;
}

#pageInfo {
    font-weight: 500;
    color: var(--text-color);
}

/* Overlay for sidebar when active */
.sidebar-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.5);
    z-index: 900;
}

.sidebar-overlay.active {
    display: block;
}

/* Responsive Design */
@media (min-width: 1201px) {
    .sidebar {
        left: 0;
    }
    .main-content {
        margin-left: var(--sidebar-width);
        width: calc(100% - var(--sidebar-width));
    }
    .hamburger {
        display: none;
    }
    .sidebar-overlay {
        display: none !important;
    }
}

@media (max-width: 768px) {
    .service-table-box {
        padding: 0;
    }

    h1 {
        font-size: 1.5rem;
        margin-top: 40px;
    }

    h2 {
        font-size: 1.3rem;
    }

    #transactionFilterForm {
        padding: 15px;
        flex-direction: column;
    }

    .form-group {
        margin: 0 0 15px 0;
        width: 100%;
    }

    select, input[type="date"] {
        font-size: 1rem; /* Fixed dropdown size */
    }

    button {
        font-size: 0.9rem;
        padding: 10px;
    }

    th, td {
        padding: 12px;
        font-size: 1rem;
    }
}

@media (max-width: 480px) {
    th, td {
        padding: 10px;
        font-size: 0.85rem;
    }

    table {
        font-size: 0.8rem;
    }
}

  </style>
</head>
<body>
    <!-- Hamburger Menu -->
    <button class="hamburger">☰</button>
    
    <!-- Sidebar Overlay -->
    <div class="sidebar-overlay"></div>
    
    <!-- Sidebar Section -->
    <div class="sidebar">
        <ul class="sidebar-menu">
          <li><a href="/admin/dashboard"><i class="fas fa-tachometer-alt"></i> <span>Dashboard</span></a></li>
          <li><a href="/admin/users"><i class="fas fa-users"></i> <span>Customers</span></a></li>
          <li><a href="/admin/category"><i class="fas fa-list"></i> <span>Categories</span></a></li>
          <li><a href="/admin/orders"><i class="fas fa-shopping-bag"></i> <span>Orders</span></a></li>
          <li><a href="/admin/products"><i class="fas fa-plus-circle"></i> <span>Add Products</span></a></li>
          <li><a href="/admin/allProducts"><i class="fas fa-box"></i> <span>Products</span></a></li>
          <li><a href="/admin/coupon"><i class="fas fa-tags"></i> <span>Coupons</span></a></li>
          <li><a href="/admin/transaction"><i class="fas fa-money-bill-wave"></i> <span>Transactions</span></a></li>
          <li><a href="/admin/complaints"><i class="fas fa-exclamation-circle"></i> <span>Complaints</span></a></li>
          <li><a href="/admin/report"><i class="fas fa-chart-bar"></i> <span>Report</span></a></li>
        </ul>
    </div>

    <div class="admin-container">
        <!-- Main Content Section -->
        <div class="main-content">
            <!-- Admin Info Box -->
            <div class="admin-info-box">
                <div class="top-header">
                    <div class="admin-info">
                        <button type="submit" class="logout-btn" onclick="confirmAdminLogout(event)">Logout</button>
                    </div>
                </div>
            </div>

            <div class="service-table-box">
                <h1>Filter Transactions</h1>

                <!-- Admin filter form -->
                <form id="transactionFilterForm">
                    <div class="form-group">
                        <label for="transactionType">Transaction Type:</label>
                        <select name="transactionType" id="transactionType">
                            <option value="">--Select Type--</option>
                            <option value="Wallet">Wallet</option>
                            <option value="Refund">Refund</option>
                            <option value="Razorpay">Razorpay</option>
                            <option value="Credit">Credit</option>
                            <option value="Debit">Debit</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="fromDate">From Date:</label>
                        <input type="date" name="fromDate" id="fromDate">
                    </div>

                    <div class="form-group">
                        <label for="toDate">To Date:</label>
                        <input type="date" name="toDate" id="toDate">
                    </div>

                    <div class="form-group" style="display: flex; align-items: flex-end;">
                        <button type="button" onclick="submitFilterForm()" style="height: 45px;">Filter Transactions</button>
                    </div>
                </form>

                <!-- Transaction list -->
                <h2>Transaction List</h2>
                <div id="transactionTable"></div>
                
                <!-- Pagination Controls -->
                <div id="paginationControls" style="display: none; margin-top: 20px; text-align: center;">
                    <button id="prevPage" style="margin-right: 10px;">Previous</button>
                    <span id="pageInfo" style="margin: 0 10px;">Page 1 of 1</span>
                    <button id="nextPage" style="margin-left: 10px;">Next</button>
                </div>

                <!-- Items per page selector -->
                <div style="text-align: center; margin-top: 10px;" hidden>
                    <label for="itemsPerPage">Items per page:</label>
                    <select id="itemsPerPage" style="padding: 5px; margin-left: 5px;">
                        <option value="10" selected>10</option>
                    </select>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Responsive sidebar toggle
        document.addEventListener('DOMContentLoaded', function() {
            const hamburger = document.querySelector('.hamburger');
            const sidebar = document.querySelector('.sidebar');
            const overlay = document.querySelector('.sidebar-overlay');
            
            hamburger.addEventListener('click', function() {
                sidebar.classList.toggle('active');
                overlay.classList.toggle('active');
            });
            
            // Close sidebar when clicking on overlay
            overlay.addEventListener('click', function() {
                sidebar.classList.remove('active');
                overlay.classList.remove('active');
            });
            
            // Active link highlight
            const sidebarLinks = document.querySelectorAll(".sidebar-menu li a");
            const currentPath = window.location.pathname;
            
            sidebarLinks.forEach(link => {
                const linkPath = new URL(link.href).pathname;
                
                if (linkPath === currentPath) {
                    link.classList.add("active");
                } else {
                    link.classList.remove("active");
                }
            });

            // Check screen width on load and resize
            function checkScreenWidth() {
                if (window.innerWidth > 1200) {
                    sidebar.classList.add('active');
                } else {
                    sidebar.classList.remove('active');
                }
            }

            // Initial check
            checkScreenWidth();

            // Add resize event listener
            window.addEventListener('resize', checkScreenWidth);
        });

        function confirmAdminLogout(event) {
            event.preventDefault();
            Swal.fire({
                title: "Are you sure?",
                text: "You will be logged out of the admin panel!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonColor: "#d33",
                cancelButtonColor: "#3085d6",
                confirmButtonText: "Yes, Logout!",
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = "/admin/logout";
                }
            });
        }

        // Transaction functionality
        let currentPage = 1;
        let totalPages = 1;

        async function submitFilterForm(page = 1) {
            const transactionType = document.getElementById('transactionType').value;
            const fromDate = document.getElementById('fromDate').value;
            const toDate = document.getElementById('toDate').value;
            const itemsPerPage = document.getElementById('itemsPerPage').value;

            const requestData = {
                transactionType,
                fromDate,
                toDate,
                page,
                limit: itemsPerPage
            };

            try {
                const response = await fetch('/admin/filterTransaction', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(requestData)
                });

                const data = await response.json();

                if (response.ok) {
                    currentPage = data.pagination.page;
                    totalPages = data.pagination.totalPages;
                    
                    renderTransactionTable(data.transactions);
                    updatePaginationControls();
                } else {
                    alert('Error: ' + data.message);
                }
            } catch (error) {
                console.error('Error submitting filter:', error);
            }
        }

        function renderTransactionTable(transactions) {
            let tableContent = '<table><thead><tr><th>Transaction ID</th><th>Amount</th><th>Transaction method</th><th>Type</th><th>Status</th><th>Date</th><th>View</th></tr></thead><tbody>';

            if (transactions.length > 0) {
                transactions.forEach(transaction => {
                    // Determine if it's Credit (for Refund) or Debit
                    const transactionTypeDisplay = transaction.transactionType === 'Refund' ? 'Credit' : 'Debit';
                    
                    tableContent += `
                        <tr>
                            <td>${transaction._id}</td>
                            <td>${transaction.amount.toFixed()}</td>
                            <td>${transaction.transactionType}</td>
                            <td>${transactionTypeDisplay}</td>
                            <td>${transaction.status}</td>
                            <td>${new Date(transaction.date).toLocaleString()}</td>
                            <td><button onclick="orderdetails('${transaction._id}')">View</button></td>              
                        </tr>
                    `;
                });
            } else {
                tableContent += `<tr><td colspan="7">No transactions found.</td></tr>`;
            }

            tableContent += '</tbody></table>';
            document.getElementById('transactionTable').innerHTML = tableContent;
        }

        function orderdetails(orderId) {
            window.location.href = `/admin/transaction/${orderId}`;
        }

        function updatePaginationControls() {
            const paginationDiv = document.getElementById('paginationControls');
            const prevBtn = document.getElementById('prevPage');
            const nextBtn = document.getElementById('nextPage');
            const pageInfo = document.getElementById('pageInfo');
            
            // Show pagination controls only if there are multiple pages
            paginationDiv.style.display = totalPages > 1 ? 'flex' : 'none';
            
            pageInfo.textContent = `Page ${currentPage} of ${totalPages}`;
            
            // Disable previous button on first page
            prevBtn.disabled = currentPage === 1;
            
            // Disable next button on last page
            nextBtn.disabled = currentPage === totalPages;
        }

        // Initialize pagination controls
        document.addEventListener('DOMContentLoaded', () => {
            // Set up event listeners for pagination buttons
            document.getElementById('prevPage').addEventListener('click', () => {
                if (currentPage > 1) {
                    submitFilterForm(currentPage - 1);
                }
            });
            
            document.getElementById('nextPage').addEventListener('click', () => {
                if (currentPage < totalPages) {
                    submitFilterForm(currentPage + 1);
                }
            });
            
            // Set up event listener for items per page change
            document.getElementById('itemsPerPage').addEventListener('change', () => {
                submitFilterForm(1); // Reset to first page when changing items per page
            });
        });
    </script>
</body>
</html>